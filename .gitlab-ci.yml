stages:
  - test

test:
  stage: test
  image: python:3.6
  before_script: []
  script:
    - pip install -r requirements.txt
    - pytest
  dependencies: []
  tags:
    - docker
  except:
    - /^wip[\/_-].+$/
